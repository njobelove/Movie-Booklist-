import { configureStore } from '@reduxjs/toolkit';

// Simple placeholder reducer
const rootReducer = {
  // Add reducers here when you have them
};

export const store = configureStore({
  reducer: rootReducer,
});